version: "3.8"

services:
  app:
    container_name: moment-app-server
    image: ${IMAGE_URI}
    ports:
      - "8080:8080"
    volumes:
      - /home/ubuntu/moment/logs:/app/logs
    env_file:
      - .env
