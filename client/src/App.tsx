import styled from '@emotion/styled';

const App = () => {
  return <AppStyled>Moment 팀</AppStyled>;
};

const AppStyled = styled.div`
  color: blue;
  font-size: 20px;
  font-weight: bold;
`;

export default App;
